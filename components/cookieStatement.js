export default function cookieStatement(){
    // Just tells users that we will use their cookies in order to store what items are in their cart since we are not making an account mandatory
    // Fade all other content slightly
    return (
        <div className="align-items-center">

        </div>
    );
}