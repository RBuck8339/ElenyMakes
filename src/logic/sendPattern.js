// From database (bucket based probably), retrieve pattern and send pattern to email
// Use thunderclient, could use C# idk how to do this tbh

// Only triggered IF purchase successful
function sendPattern({email}){
    // Retrieval


    // Sending if retrieval successful
}